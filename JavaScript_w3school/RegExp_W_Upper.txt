定义和用法
\W 元字符用于查找非单词字符。
单词字符包括：a-z、A-Z、0-9，以及下划线。

语法
new RegExp("\W")

直接量语法：
/\W/

浏览器支持
所有主流浏览器都支持 \W 元字符。

实例
对字符串中的非单词字符进行全局搜索：
var str="Give 100%!";
var patt1=/\W/g;
下面被标记的文本显示了表达式获得匹配的位置：
Give 100%!
